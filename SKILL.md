---
name: x-knowledge-illustrations
description: 用固定 IP「X」把中文文章、公众号、帖子、课程内容和方法论转成蓝黑手绘插画、口播文案或图文口播脚本。适用于文案拆解、shot list、逐页插画、短视频口播、分镜字幕、观点视觉化、连续成组出图和基于 X 角色参考图的改图；不用于正式架构图、数据图表、写实商业海报或未经来源支持的事实扩写。
---

# X 知识插画

把中文内容里的关键判断、关系、流程、状态和认知转折，转成清爽、有记忆点的 16:9 横版正文插图、自然口语化的口播文案，或画面与口播逐页对应的视频脚本。图像应帮助读者理解观点，口播应帮助听众跟上思路，而不是把原文朗读一遍或排成一页 PPT。

固定角色是知识火柴人 **X**：圆头、圆框眼镜、头顶一小撮头发、黑色细线身体、钴蓝色领巾。X 是好奇、克制、可靠的知识探索者，负责思考、讲解、拆解、连接、观察或携带知识。

## 参考资源

按任务读取，不要一次加载全部：

- 需要稳定角色时，先读 [references/x-ip.md](references/x-ip.md)，并查看 `assets/x-ip-reference.png`。
- 需要确定整体画风时，读 [references/style-system.md](references/style-system.md)。
- 需要为文章选图或设计构图时，读 [references/composition.md](references/composition.md)。
- 准备调用生图或改图工具时，读 [references/prompt-guide.md](references/prompt-guide.md)。
- 需要口播、短视频脚本、逐页旁白或字幕时，读 [references/voiceover.md](references/voiceover.md)。
- 生成后检查时，读 [references/qa.md](references/qa.md)。

## 选择工作模式

根据用户的交付物选择一种模式，不要默认把所有能力同时使用：

1. **插画模式**：输出配图策略、shot list 或逐张图片。
2. **口播模式**：只输出可以直接朗读的完整口播稿；除非用户要求，不生成图片。
3. **图文口播联动模式**：把同一条叙事拆成连续页面，每页同时提供画面、口播、屏幕短字、时长和转场；用户要求生成图片时再逐页调用图像工具。

用户只说“改成口播”时，默认使用口播模式。用户同时提到“画面、分镜、逐页、视频、字幕”时，优先使用图文口播联动模式。

## 工作流

### 1. 提炼认知锚点

先读正文或用户给出的主题，找出真正值得画的内容：

- 核心判断或认知转折
- 输入、处理、输出之间的关系
- 前后状态变化
- 容易误解的区别
- 抽象概念可对应的物理动作
- 能由 X 亲自完成的解释行为

不要按段落平均配图。短文通常 1-3 张，长文通常 4-8 张；信息不足时宁少勿滥。

### 2. 决定输出形态

- 用户要求“分析怎么配图、给建议、shot list”时，只输出配图策略。
- 用户明确要求“生成、做图、输出图片”时，完成必要分析后直接生成，不要停在建议阶段。
- 用户要求“口播、旁白、视频文案”时，先确定或合理推断目标时长，再按 `references/voiceover.md` 写成可以直接朗读的口语稿。
- 用户要求“逐页画面 + 口播”时，每页绑定一个认知锚点，口播只解释当前画面承担的观点，不提前讲后续页面。
- 每张图单独生成，不把多张正文图拼成一张总图。

shot list 每项只需说明：插入位置、核心意思、构图类型、X 的动作、建议短标注。

图文口播联动稿每页包含：页码、核心观点、画面设计、口播、屏幕短字、建议时长、转场。最终补充预计总时长和口播总字数。

### 3. 使用 X 的真实参考图

生成或编辑角色图时，优先把 `assets/x-ip-reference.png` 作为图像参考传给可用的图像工具，而不是只用文字描述。第一次使用前先查看参考图。

保留角色识别点：圆框眼镜、头顶小发梢、蓝色领巾、黑色火柴人身体、温和专注的表情。蓝色书本是常用道具，不是每张必需。除非用户要求，不要在画面上写角色名字或做品牌角标。

### 4. 一图一意

每张图只表达一个核心关系。让 X 承担关键动作，例如：

- 思考：面对问题、比较线索、形成假设
- 讲解：指向简化图示、带读者看重点
- 拆解：把复杂问题拆成 3-5 个可见部分
- 连接：把两端、证据与结论、问题与方法连起来
- 观察：用放大镜寻找差异、异常或证据
- 携带知识：带着书本、卡片或知识块移动

如果去掉 X 后画面含义完全不变，说明角色只是装饰，应重写构图。

### 5. 生成与检查

使用可用的图像生成工具，遵守 `references/prompt-guide.md`。默认视觉要求：

- 16:9 横版，纯白背景
- 黑色手绘线条 + 钴蓝色强调色
- 大量留白，主体清晰
- 最多 3-6 个简短中文标注
- 非 PPT、非复杂信息图、非写实、非 3D

生成后按 `references/qa.md` 检查。角色漂移、文字错误、画面拥挤或像课件时，优先局部编辑；整体结构错误时重新生成。

口播稿需要单独检查：开场是否迅速提出矛盾，句子能否顺口朗读，页面之间是否自然递进，是否保留原文立场与证据边界，以及结尾是否形成清晰收束。不要把文章小标题逐个念出来。

### 6. 保存交付

在 workspace 中工作时，保存到：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

不要覆盖已有资产，除非用户明确要求替换。交付时简要列出图片数量、用途和绝对路径。

口播或图文联动稿保存为：

```text
assets/<article-slug>-illustrations/voiceover-script.md
```

如果只交付口播文案而没有 workspace，直接在回复中给出完整可朗读版本。
